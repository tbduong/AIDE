<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>
  <%= stylesheet_link_tag    "application" %>
  <%= javascript_include_tag "application", 'data-turbolinks-track', 'vendor/modernizr' => true %>
  <%= csrf_meta_tags %>
  <%= include_uploadcare_widget_from_cdn version: "2.9.0", min: true %>
  <%= uploadcare_settings %>

  <div class="top-bar">
    <div class="top-bar-left">
      <ul class="menu" data-dropdown-menu>
        <li class="menu-text"> <%= link_to "[ A I D E ]", root_path, title:'AIDE logo' %></li>
        <li class="menu-text"> <%= link_to "About Us", about_path, title:'About Us' %> </li>
        <li class="menu-text"><%= link_to "Resources", resources_path, title:'Resources' %></li>
        <% if current_user %>
        <li class="menu-text"> <%= link_to "Caregivers Directory", caregivers_path, title:'Caregivers Directory' %> </li>
        <% end %>
      </ul>
    </div>

    <div class="top-bar-right">
      <ul class="menu">
        <% if current_user %>
          <% if current_user.patient %>
            <li class="menu-text"> <%= link_to "Profile", patient_path(patient.id), title:'User Profile Page' %> </li>
          <% else %>
            <li class="menu-text"> <%= link_to "Profile", caregiver_path(caregiver.id), title:'User Profile Page' %> </li>
            <% end %>
        <li class="menu-text"> <%= link_to "Logout", logout_path, title:'Logout Button' %> </li>
        <% else %>
        <li class="menu-text"> <%= link_to "Login", login_path , title: 'Login Button'%> </li>
        <li class="menu-text"> <%= link_to "Sign Up", new_user_path, title: 'Sign Up Button' %> </li>
      </ul>
    </div>
    <% end %>
  </div>
<br>
  <div class="row">
    <div class="flash-message text-center">
      <% flash.each do |type, m| %>
      <%= m %>
      <% end %>
    </div>
  </div>
</head>
<body>
  <br>

  <%= yield %>

</body>
</html>
